# SECURITY / ABUSE / PRIVACY

## QR 공유
위협:
- 고객이 매장 QR 사진을 외부에 공유

대응:
- 매장 + 전화번호 기준 2일 캘린더 쿨타임
- 미사용 쿠폰이 있으면 신규 게임 불가
- 쿠폰 사용 처리에는 직원 PIN이 필수
- QR 토큰은 추측 불가능한 난수이며 관리자 화면에서 즉시 폐기·재발급 가능

게임 시작 자체에는 직원 확인을 두지 않는다(운영 편의를 위한 결정). 따라서 QR이 외부에 유출되면
모르는 사람도 게임에 참여해 쿠폰을 발급받을 수 있으며, 실제 손해는 쿠폰 사용 단계의 직원 PIN에서 막는다.

## 직원 PIN 유출
MVP 대응:
- 매장별 고유 6자리
- 쿠폰 사용 처리에만 사용
- 관리자에서 재발급
- 오입력 Rate Limit
- 서버 로그에 PIN 기록 금지

향후:
- 직원별 계정
- 승인 버튼
- 1회성 직원 승인 code

## PIN 저장
평문 DB 저장 금지 권장.
```text
BCrypt 또는 Argon2 Hash
```

## PIN Rate Limit
권장 시작값:
```text
IP + storeToken 기준 1분 10회
```
5회 이상 연속 실패 시 짧은 cooldown 추가 가능.

## 전화번호
전화번호를 중복 참여 판단에 사용하므로 보호 저장.

```text
phone_hash      HMAC-SHA256
phone_encrypted AES-GCM
phone_last4     마지막 4자리
```

로그에 전체 전화번호를 남기지 않는다.

DB에는 이름과 정규화 전화번호를 AES-256-GCM으로 암호화하며 레코드마다 새 96-bit nonce를 사용한다. 중복 참여 조회는 별도 HMAC-SHA256 값으로 수행하고 관리자 화면에는 전화번호 끝 4자리만 제공한다. 암호화 키와 HMAC 키는 서로 분리된 필수 환경변수이며 저장소에 커밋하지 않는다.

## 관리자 인증

- 관리자 계정은 소문자로 정규화한 이메일 하나로 식별하며 로그인에도 이메일만 쓴다.
- 손으로 넣는 값은 서버의 `Inputs`에서 한 번에 정규화·검증한다. 휴대전화는 숫자만 남겨 `010`으로
  시작하는 11자리, 사업자등록번호는 숫자 10자리만 통과한다. 화면에서도 같은 규칙으로 막지만
  판단은 서버가 한다.
- 한 계정이 만들 수 있는 매장 수에 상한을 둔다. 인증된 사용자가 매장을 무한히 만들지 못하게
  하는 선이며 정책이 아니라 안전장치다.
- 매장 회원가입 비밀번호는 영문과 숫자를 포함해 10자 이상을 요구하고 비밀번호 확인을 서버에서 재검증한다.
- 사업자등록번호는 숫자 10자리로 정규화해 저장하며 매장 단위로 유일해야 한다.
- 비밀번호는 BCrypt hash만 저장하고 bootstrap 비밀번호는 12자 이상을 요구한다.
- 로그인 실패는 동일 ingress 기준 분당 5회로 제한한다.
- JWT secret은 최소 32자이며 환경변수 누락 시 시작을 실패시킨다.
- 관리자 매장 API는 JWT claim만 믿지 않고 DB의 현재 역할과 매장 membership을 매 요청 재검증한다.
- 관리자 JWT는 브라우저 sessionStorage에만 두며 로그아웃/브라우저 종료 시 폐기한다.

## 운영자 콘솔 (2026-09-18)

플랫폼 전체를 보는 화면은 매장 관리자 화면과 완전히 분리한다. 주소는 `/admin/system`(화면)과
`/api/system/**`(API)이며, 매장 콘솔 어디에도 이 화면으로 가는 링크를 두지 않는다.

### 들어오는 문

- **비밀번호만으로는 열리지 않는다.** SYSTEM_ADMIN 계정도 TOTP(RFC 6238, SHA-1/30초/6자리) 2단계
  인증을 등록해야 콘솔 토큰을 받는다. 등록 전에는 로그인 대신 등록 안내만 돌려준다.
- TOTP 비밀값은 AES-256-GCM으로 암호화해 저장한다. DB 덤프만으로 두 번째 인증 수단이 복제되면
  2단계가 아니라 비밀번호 두 개다.
- 한 번 쓴 코드는 다시 받지 않는다(시간 슬롯 기록). 시계 오차는 앞뒤 30초까지만 허용한다.
- 등록을 마치면 **복구 코드 10개**를 그 자리에서 한 번만 보여 준다. BCrypt 해시로만 저장하고 한 코드는
  한 번만 쓸 수 있다. 폰을 잃어버린 운영자가 스스로 돌아올 유일한 길이며, 이게 없으면 운영자가 한 명인
  서비스는 통째로 잠긴다.
- 로그인 실패는 IP당 5분에 5회, 계정당 5분에 10회(프로세스 안). 여기에 더해 연속 10회 실패면 계정이
  15분 잠기고, 이 잠금은 **DB에 남아 재시작으로 풀리지 않는다.**
- 없는 계정·틀린 비밀번호·운영자가 아닌 계정은 모두 같은 오류를 받는다. 잠금·중지·IP 차단은
  비밀번호가 맞은 뒤에만 알려 준다. 어떤 이메일이 운영자인지 알려 주지 않기 위해서다.
- 운영자 비밀번호는 영문·숫자·기호를 포함해 12자 이상이며 이메일과 겹칠 수 없다. 남이 만들어 준
  임시 비밀번호는 본인이 바꾸기 전까지 조회조차 막힌다(`PASSWORD_CHANGE_REQUIRED`).
- 비밀번호를 바꾸면 다른 세션은 모두 닫힌다. 바꾸는 이유가 대개 "누가 알아낸 것 같다"이기 때문이다.

### 세션

- 콘솔 토큰은 `scope=OPERATOR`를 가진 별도 토큰(기본 60분)이며 **서버가 들고 있는 세션 행**을
  가리킨다(`jti`). 그래서 로그아웃·강제 종료·권한 회수가 토큰 만료를 기다리지 않는다.
- 유휴 15분이면 서버가 세션을 닫는다. 화면 타이머는 안내일 뿐이고 판단은 서버가 한다.
- 토큰이 다른 기기에서 쓰이면(User-Agent 해시 불일치) 세션을 닫고 기록한다. UA 원문은 저장하지 않는다.
- 한 계정의 동시 세션은 5개까지. 넘치면 가장 오래된 것부터 닫는다.
- 매장 콘솔 로그인으로 받은 토큰은 SYSTEM_ADMIN의 것이라도 `/api/system/**`을 열지 못한다.
  보관 위치도 `opsToken`/`adminToken`으로 나눈다(둘 다 sessionStorage).
- 요청마다 DB의 역할·계정 상태·세션 상태를 다시 본다.

### 권한과 재인증

- 콘솔 안에도 등급이 있다. `VIEWER`(조회) < `OPERATOR`(매장 요금제·상태 변경) < `OWNER`(운영자 계정
  관리, 기록 검증). 콘솔 밖에서 만들어진 운영자 계정은 활성 OWNER가 이미 있으면 VIEWER로 시작한다.
- 위험한 조작(요금제, 매장 중지, 계정 관리, 복구 코드 재발급, 남의 세션 종료)은 최근 5분 안에 2단계
  인증을 통과한 세션에서만 된다. 열어 둔 탭이나 빌려준 노트북이 곧바로 그 권한이 되지 않게 한다.
- 마지막 OWNER는 권한을 내리거나 중지할 수 없고, 자기 자신의 권한도 내릴 수 없다.
- 권한을 내리거나 계정을 중지하면 그 계정의 세션이 즉시 닫힌다.
- 계정별 접속 허용 IP를 둘 수 있다(전역 `SYSTEM_CONSOLE_ALLOWED_IPS`와 함께 적용).

### 앞단과 노출

- `SYSTEM_CONSOLE_ALLOWED_IPS`(단일 IP 또는 CIDR) 밖의 주소는 로그인 화면에도 닿지 못한다. 차단은
  403이 아니라 **404**로 답해 경로의 존재를 알리지 않는다. `SYSTEM_CONSOLE_ENABLED=false`면 경로 전체가
  404다. 쓰지 않는 기간에는 꺼 둔다.
- Cloudflare 터널 뒤에서는 모든 요청이 터널 IP로 보여 IP 제한이 무의미해진다. 터널을 쓸 때 IP 제한이
  필요하면 Cloudflare Access 같은 앞단 인증을 함께 둔다.
- Nginx가 `/api/system/auth/`에 분당 10회(버스트 5) 제한을 한 겹 더 건다.
- 콘솔 응답에는 `X-Robots-Tag: noindex`와 `Cache-Control: no-store`가 붙고, 화면에도 noindex를 건다.

### 기록

- 로그인 성공·실패, 재인증, 세션 종료, 비밀번호 변경, 요금제·매장 상태 변경(사유 필수), 운영자 계정
  변경, 권한 없는 시도, 기록 내보내기가 모두 감사 로그에 남는다. 고객 개인정보는 들어가지 않는다.
- 각 행은 앞 행의 해시를 품는다(해시 사슬). 중간을 고치거나 지우면 `GET /api/system/audit/verify`가
  어디서 끊겼는지 짚어 준다. 마지막 행부터 잘라내는 것까지 막지는 못한다. 무결성을 보장하는 장치가
  아니라 **조용히 고칠 수 없게** 하는 장치다.
- 실패한 요청의 기록과 실패 횟수는 그 요청과 함께 롤백되면 안 된다. 그래서 감사 기록은 별도
  트랜잭션으로 쓰고, 로그인·세션 검사는 트랜잭션에 묶지 않는다.
- CSV로 내보낸 칸이 스프레드시트에서 수식으로 실행되지 않게 `=`, `+`, `-`, `@`로 시작하는 값 앞에
  작은따옴표를 붙인다.

### 볼 수 있는 것

- 운영자가 보는 것은 매장 단위 집계까지다. 손님 이름과 전화번호는 그 매장 관리자만 본다.
- 운영자가 매장의 상품·확률·직원 PIN·참여자 명단을 대신 바꾸는 엔드포인트는 만들지 않는다.
  운영자용 뒷문은 공격자에게도 뒷문이다.

### 계정 만들기

- 첫 운영자는 `SYSTEM_ADMIN_EMAIL`/`SYSTEM_ADMIN_PASSWORD`(12자 이상)로 한 번 만들어지며 OWNER다.
  이미 있는 계정의 비밀번호를 환경변수로 덮어쓰지 않는다. 덮어쓰면 그 변수를 읽을 수 있는 사람이
  곧 운영자가 된다.
- 그다음부터는 콘솔의 운영자 화면에서 만든다. 누가 언제 누구에게 무슨 권한을 줬는지 남기기 위해서다.

## 게임 조작 방지
금지:
```text
Frontend Math.random()으로 결과 생성
```

필수:
- 서버 SecureRandom
- 결과 DB 선저장
- Idempotency

윷 결과의 가중치와 등급 매핑은 매장이 설정하지만, 설정은 관리자 인증 API로만
바꿀 수 있고 저장 전 서버에서 검증한다. 고객 요청은 가중치, 결과, 등급 중
어느 것도 보낼 수 없고 서버는 그런 필드를 받지 않는다.

설정 변경이 이미 발급된 쿠폰을 소급해 바꾸지 못하게, 쿠폰은 발급 시점에
등급과 상품 정보를 스냅샷으로 동결한다. 사장이 뒤늦게 확률이나 상품을 바꿔도
고객이 이미 받은 쿠폰은 그대로다.

고객에게 노출하는 확률은 서버가 계산한 값만 쓴다. 가중치가 0이라 나올 수 없는
등급은 목록에서 제외해, 받을 수 없는 상품을 확률과 함께 광고하지 않는다.

## 쿠폰 조작 방지
쿠폰 URL은 순차 ID가 아닌 긴 난수 Token 사용.

사용 처리:
- 상태 `ISSUED`인지 확인
- validFrom/expiresAt 확인
- 해당 매장 PIN 확인
- Atomic update 또는 DB Lock

## 매장 간 권한
모든 Admin Store API는 Backend에서 Membership 확인.

```text
로그인 계정이 Store A 소유자라도
Store B API는 403
```

## 개인정보 동의
게임 전 개인정보 수집 동의 필수.

수집 항목:
- 이름
- 전화번호
- 참여 매장
- 게임 결과
- 당첨상품/쿠폰 상태
- 참여/사용 시각

보유기간은 참여일을 포함해 120일이다. `Asia/Seoul` 날짜 기준 120일이 지난 다음 날부터
이름 암호문, 전화번호 암호문·HMAC·끝 4자리를 하루 한 번 배치로 복구 불가능한 값으로
덮어쓴다. 아직 유효한 `ISSUED` 쿠폰이 연결된 참여는 기능 보존을 위해 만료 뒤 처리한다.
매장, 결과, 상품 등급, 참여일, 쿠폰 상태·상품 스냅샷 같은 비식별 통계 필드는 유지한다.
배치는 제한된 건수씩 처리하며 재실행해도 이미 익명화된 행은 바꾸지 않는다.

## 리뷰 운영
MVP는 특정 별점이나 긍정적인 내용 작성을 시스템적으로 요구하지 않는다.
직원은 리뷰 `작성 여부`만 확인하는 구조로 둔다.
출시 직전 네이버 및 관련 리뷰/광고 정책 최신본을 별도 점검한다.

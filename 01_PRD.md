# PRD - 매장 이벤트 윷놀이 서비스

## 1. 제품 목적

매장이 리뷰 작성 여부와 무관하게 QR 이벤트를 열고, 참여·당첨·쿠폰 사용·재참여 데이터를 한곳에서 운영하게 한다. 고객은 앱 설치나 로그인 없이 참여하고, 매장은 종이 추첨이나 수기 쿠폰보다 일관되고 검증 가능한 경험을 제공한다.

네이버 플레이스 링크는 매장이 원할 때 함께 보여 주는 선택 동선이다. 리뷰, 별점, 특정 문구는 참여나 혜택의 조건이 아니다.

## 2. 사용자

### CUSTOMER

- QR 접속
- 선택적으로 네이버 플레이스 매장 정보 열기
- 이름·전화번호 입력과 개인정보 동의
- 미사용 쿠폰 조회
- 윷놀이 참여
- 당첨 상품 조회와 사용

### STORE_ADMIN

- 직접 가입하고 복수 매장 관리
- 매장별 1~5개 상품 등급과 결과별 가중치 설정
- 상품 사용 시점과 쿠폰 사용 기한 설정
- 선택 네이버 플레이스 URL과 안내 문구 설정
- QR 안내물 저장·공유, 직원 PIN 재발급
- 참여·쿠폰·통계 조회

### SYSTEM_ADMIN

- 매장 활성·비활성 관리
- 결제 연동 전 구독 등급 운영

## 3. 고객 신규 참여 시나리오

1. 고객이 매장 전용 QR을 스캔한다.
2. 이벤트 안내와 상품을 확인한다.
3. 네이버 링크가 있으면 매장 정보를 선택적으로 열 수 있다.
4. 이름·전화번호를 입력하고 개인정보 수집에 동의한다.
5. 서버가 미사용 쿠폰을 먼저 조회한다.
6. 미사용 쿠폰이 없으면 최근 참여일을 조회한다.
7. 2일 쿨타임이 끝났으면 게임을 생성한다.
8. 서버에서 결과를 먼저 결정하고 저장한다.
9. 3D 윷 애니메이션을 재생한다.
10. 결과를 공개하고 쿠폰을 발급한다.

게임 입장에는 직원 PIN이나 리뷰 확인이 필요하지 않다. 직원 PIN은 쿠폰 사용 처리에만 쓴다.

## 4. 미사용 쿠폰 보유 시

고객 식별 후 아직 사용할 수 있는 `ISSUED` 쿠폰이 있으면 새 게임보다 기존 쿠폰 화면을 먼저 보여 준다.

## 5. 재참여 제한

식별 기준은 `store_id + phone_hash`다.

- 최근 참여일: 2026-09-02
- 재참여 가능일: 2026-09-04 00:00 Asia/Seoul

정확히 48시간이 아니라 달력 날짜 기준 2일이다.

오류 예시:

> 이미 최근에 이벤트에 참여하셨습니다. 9월 4일부터 다시 참여하실 수 있습니다.

## 6. 게임 결과와 확률

물리 결과는 도 `DO`, 개 `GAE`, 걸 `GEOL`, 윷 `YUT`, 모 `MO` 다섯 가지로 고정한다. 매장은 각 결과의 가중치와 연결할 상품 등급을 설정한다.

기본값:

| 결과 | 가중치 | 상품 등급 |
|---|---:|---:|
| 도 | 325 | 3등 |
| 개 | 325 | 3등 |
| 걸 | 125 | 2등 |
| 윷 | 125 | 2등 |
| 모 | 100 | 1등 |

서버는 다음을 검증한다.

- 가중치는 각각 0..1000
- 가중치 합은 1 이상
- 사용 등급은 연속된 `1..N`, `N <= 5`
- 사용 등급마다 활성 상품이 존재
- 가중치가 0인 결과는 발생하지 않음

결과는 서버의 보안 난수로 결정한다. 고객 요청은 가중치, 결과, 등급을 보내거나 선택할 수 없다.

## 7. 상품과 이벤트 설정

매장은 1~5개 상품 등급을 사용하며 1이 최고 등급이다. 관리자 화면은 3·4·5등급 프리셋도 제공한다.

각 상품 필드:

- 등급 `rank`
- 상품명
- 설명
- 사용 정책
- 활성 여부

사용 정책:

- `SAME_DAY`: 발급일부터 사용 가능
- `NEXT_DAY`: 다음날 00:00부터 사용 가능
- `ANYTIME`: 발급 직후부터 사용 가능

상품과 결과별 가중치는 `PUT /api/admin/stores/{storeId}/event-configuration`으로 전체 문서를 원자 저장한다. 먼저 5개 결과와 사용 등급 `1..N`의 상품을 모두 검증하고, 하나라도 잘못되면 아무 변경도 적용하지 않는다.

## 8. 쿠폰

상태:

- `ISSUED`
- `REDEEMED`
- `EXPIRED`
- `CANCELLED`

쿠폰 사용 기한은 매장이 1~365일로 설정하며 기본값은 90일이다. 사용할 수 있게 된 날부터 N일째 23:59:59까지 유효하다. 설정 변경은 이후 발급되는 쿠폰에만 적용된다.

쿠폰은 발급 시 상품 등급, 이름, 설명, 사용 정책을 스냅샷으로 저장한다. 이후 상품·확률·기한 설정을 바꿔도 이미 발급된 쿠폰은 변하지 않는다.

## 9. 직원 PIN

- 매장 생성 시 6자리 숫자 난수 생성
- 매장별 고유
- 쿠폰 사용 처리에만 사용
- 관리자에서 재발급 가능
- 오입력 Rate Limit 적용
- 평문 저장·로그 금지

## 10. 매장 QR과 안내물

공개 URL 예:

```text
https://yut.sodamlabs.kr/s/{publicToken}
```

예측 가능한 `storeId`를 노출하지 않는다. QR은 폐기·재발급할 수 있다. 안내물은 현재 공개 origin, 활성 QR 토큰, 매장명과 선택 안내 문구로 생성한다.

## 11. 구독과 AI

- `BASIC`, `STANDARD`, `PRO` 세 요금제가 있다.
- 게임, QR, 상품·확률, 쿠폰, 직원 PIN, 쿨타임은 모든 요금제에서 동일하다.
- 차등은 분석 깊이, CSV, 브랜딩과 AI에만 둔다.
- 고객 API에는 AI 진입점이 없다.
- AI에는 집계와 공개 라벨만 전달하며 고객 개인정보는 전달하지 않는다.
- 결제 PG는 현재 범위 밖이며 구독 변경은 운영자 API로만 처리한다.

## 12. 비기능 요구사항

- 모바일 우선
- 게임 중 새로고침해도 결과 유지
- 게임 생성·쿠폰 발급·결과 공개 멱등성
- 쿠폰 사용 원자 처리
- 고객 이름·전화번호 보호 저장
- HTTPS 필수
- 매장 간 데이터와 권한 격리
- AI 장애가 고객 게임·쿠폰 흐름에 전파되지 않음

## 13. 현재 범위 밖

- PG 결제
- Solapi 알림톡과 SMS 인증
- 상품 재고 제한
- 직원별 계정
- 고객용 AI 챗봇
- 자동 리뷰 작성
- 리뷰·별점·긍정 표현을 혜택 조건으로 만드는 기능

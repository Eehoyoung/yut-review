# 구독 권한 · 소담 AI 작업보고서

- 대상: `yut-review-subscription-ai-codex-prompt-v2.json`
- 기간: 2026-09-04
- 저장소: https://github.com/Eehoyoung/yut-review (`main`)
- 결과: **지시 항목 전부 구현 완료.** 테스트 56개(백엔드 40 · 프런트 16) 통과, E2E 26개 체크 통과.

---

## 1. 무엇을 만들었나

윷리뷰를 매장 운영 데이터를 분석해 주는 SaaS로 넓혔다. 두 축이다.

**요금제 3단계** — BASIC 9,900 / STANDARD 19,900 / PRO 29,900원.

**소담 AI 네 기능** — 안내 문구, 운영 리포트, 개선 제안, AI 매니저.

가장 중요한 설계 결정은 **게임과 핵심 고객 경험을 등급으로 나누지 않는다**는 것이다. 이건 주석이
아니라 코드 구조로 못박았다. `Entitlement` enum에 윷놀이·QR·상품·쿠폰·직원PIN·쿨타임이 **아예 없다.**
목록에 없다는 사실 자체가 "등급으로 팔 수 없다"는 뜻이고, 테스트가 그 목록을 고정한다.

```java
// 이 목록이 전부다. 게임 관련 항목을 여기 추가하는 순간 팔 수 있는 것이 된다.
enum Entitlement { BASIC_ANALYTICS, ADVANCED_ANALYTICS, CSV_EXPORT, BRANDING }
```

---

## 2. 커밋

| 커밋 | 내용 |
|---|---|
| `50229a0` | 요금제 권한 · AI 기능 백엔드 |
| `8a0c4b8` | 요금제 화면 · 관리자 AI 도우미 |
| `3e515fd` | 규칙 문서화 |
| `2788ee4` | 1차 리뷰 결함 보완 |
| `82f3f34` | 2차 리뷰 결함 보완 + 미구현 요금제 기능 완성 |
| `010d0da` | 요금제로 갈리는 통계 화면과 시간대 분포 |
| `03d583a` | AI·요금제 화면을 기능 성격에 맞는 형태로 |
| `a57a9b5` | 관리자 자유 입력 PII 차단 |
| `14f0c0f` | 문서 최신화 |

---

## 3. 지시 항목 대조

### 요금제

| 항목 | 상태 | 비고 |
|---|---|---|
| BASIC/STANDARD/PRO 3단계 | 완료 | 구독 행 없으면 BASIC으로 본다(백필 불필요) |
| 기능별 열림/잠김 | 완료 | 402 `PLAN_UPGRADE_REQUIRED` |
| BASIC 분석 보관 90일 | 완료 | 지시대로 30일이 아닌 90일 |
| STANDARD 365일 / PRO 장기 | 완료 | |
| 월 AI 쿼터 | 완료 | 조건부 UPDATE로 원자적 차감 |
| PRO 주간 자동 리포트 | 완료 | 월요일 04:20 KST |
| 고객 PII 보존 120일 유지 | 완료 | 요금제와 무관, 기존 정책 그대로 |
| PG 결제 미구현 | 준수 | 등급 변경은 운영자 조작 |

### AI

| 항목 | 상태 | 비고 |
|---|---|---|
| LlmProvider 추상화 | 완료 | Fake 기본 / OpenAI Responses API |
| 환경변수 5종 | 완료 | `AI_PROVIDER` `OPENAI_API_KEY` `AI_MODEL_FAST/ANALYSIS/CHAT` |
| Structured Outputs | 완료 | 문구·리포트·개선 제안 |
| AI_EVENT_COPY (STANDARD+) | 완료 | 월 20/100회 |
| AI_REPORT (STANDARD+) | 완료 | 월 5/20회 |
| AI_IMPROVEMENT (PRO) | 완료 | 월 30회 |
| AI_CHAT (PRO) | 완료 | 월 100회, 도구 7종 |
| 집계 도구 7종 | 완료 | 전부 읽기 전용, storeId 인자 없음 |
| 사용량·비용 기록 | 완료 | 단가는 설정값, 미설정이면 비용을 만들지 않음 |
| AI 장애 격리 | 완료 | 진입점이 관리자 API에만 존재 |

### 금지 항목

| 금지 | 준수 |
|---|---|
| PG 결제 연동 | 미구현 |
| 고객용 AI 챗봇 | 고객 API에 AI 엔드포인트 0개 |
| 리뷰 자동 작성 | 없음 |
| 긍정 리뷰/5점 유도 | 시스템 프롬프트가 명시적으로 금지 |
| AI에 고객 개인정보 전송 | 차단 + 테스트로 고정 |
| 윷 확률/3D/쿨타임/PIN 변경 | 해당 코드 미수정 |

---

## 4. 개인정보를 지킨 방법

지시의 `critical_data_rule`은 이 작업에서 가장 무거운 요구였다. 두 겹으로 지켰다.

**첫째, 통로를 하나로 좁혔다.** `AiContextService`가 LLM 입력을 만드는 유일한 자리다. 엔티티를
직렬화하지 않고 집계 쿼리 결과만 옮긴다. 새 필드를 넣을 때도 "이 값이 한 사람을 가리킬 수 있는가"를
먼저 묻게 된다.

**둘째, 테스트가 실제로 나간 문자열을 검사한다.** 실제 참여 데이터를 넣고 리포트를 만든 뒤, 모델에
간 문자열에서 이름·전화번호·해시·토큰·필드명을 찾아 없음을 확인한다. 규칙을 문장으로 적어 두는 것과
값이 실제로 안 나갔는지 보는 것은 다르다.

**작업 중 발견한 구멍이 하나 있었다.** 자유 입력이다. 사장이 "010-1234-5678 손님한테 보낸 문구처럼"
이라고 적으면 그대로 모델에 갔다. 유일하게 검증이 없는 유입 경로였다. 전화번호가 될 만한 숫자
덩어리와 이메일을 거부한다. 마스킹하지 않고 거부하는 이유는, 가려서 보내면 사장은 자기가 쓴 내용이
그대로 갔다고 믿은 채 남기 때문이다.

---

## 5. 리뷰 사이클에서 잡힌 것

두 차례 리뷰를 돌렸다. **셋은 그대로 나갔으면 실제로 터졌다.**

### 1차

| 결함 | 무엇이 터지나 |
|---|---|
| 매장주가 스스로 PRO로 올릴 수 있었음 | 요금제 시스템 전체 우회, 운영자 API 키로 AI 호출 무제한 |
| 모델 호출이 DB 트랜잭션 안 | 45초 대기 동안 커넥션 점유 → 풀 고갈 → **손님 게임·쿠폰 실패** |
| 채팅이 도구 결과를 매 라운드 폐기 | 근거를 비운 채 답을 요구 = 숫자를 지어내라고 등을 떠미는 구조 |
| 한도 되돌리기가 읽고-쓰기 | 원자적 차감이 무의미해져 한도 초과 |
| 과금된 실패도 환급 | 실패 반복 시 사용량은 그대로, 비용만 나감 |
| 보관기간 밖 조회가 "참여 0건" | 모델이 이벤트가 죽었다고 분석 |
| 시간대 집계가 세션 타임존 의존 | H2와 운영이 9시간 어긋남 |

### 2차 — 1차 보완이 만든 새 결함

| 결함 | 무엇이 터지나 |
|---|---|
| `AI_PROVIDER` 오타가 여전히 앱을 죽임 | 실증됨(`azure`로 18/18 실패). 관리자 설정 실수가 손님 흐름까지 중단 |
| 운영자 전용 수정이 운영자까지 차단 | 멤버십 검사가 먼저라 아무도 등급을 못 바꿈 |
| 읽기 타임아웃을 여전히 환급 | 이미 과금된 호출 |
| 도구 누적 테스트가 무의미 | 라운드가 한 번만 돌아 고치기 전 코드로도 통과 |
| 상한에서 도구를 한 번 더 실행 | 계약상 4회를 넘김 |

**두 번째 표가 이 작업에서 가장 중요한 기록이다.** 첫 보완이 새 결함을 만들었고, 그중 하나는
"고쳤다"고 커밋 메시지에 적은 것이 사실이 아니었다. 리뷰를 한 번만 돌렸으면 그대로 나갔다.

내가 직접 잡은 것도 둘 있다. **AI·요금제 문자열이 고객 번들에 두 번 새어 나갔다** — 한 번은 공용
`labels.ts`, 한 번은 공용 `api.ts`. 둘 다 고객 화면이 함께 import하는 모듈이다. 빌드 산출물에서 고객
청크 17개를 매번 검사하는 것으로 확인한다.

---

## 6. 검증

```
백엔드     40개 (CoreRulesTest 15 + SubscriptionAiTest 25)
프런트     16개 (윷 던지기 시뮬레이션 8 포함, 변경 없음)
E2E        26개 체크 (실제 스택 localhost:8088)
번들 격리  고객 청크 17개에 요금제·AI 문자열 0
```

특별히 확인한 것:

- **동시성** — 스레드 26개로 밀어 넣어 PRO 한도 20건에서 정확히 20건만 통과
- **공급자 오설정** — `APP_AI_PROVIDER=azure`로 전체 테스트 재실행, 40개 그대로 통과
- **AI 장애 중 고객 흐름** — 공급자가 죽은 상태에서 손님이 윷을 던져 쿠폰을 받는 것까지
- **CSV 내용** — 참여자 이름·전화번호가 한 칸도 없음
- **차트 색 대비** — dataviz 검증기로 계산. 옅은 막대가 2.26:1이라 밝은 매장에서 안 보여 3:1을 넘는
  색으로 올렸다

---

## 7. 화면

**통계** — BASIC은 요약만 보고, 왜 더 못 보는지와 그 숫자가 어디 쓰이는지를 함께 알려 준다("알바
배치나 이벤트 시간을 정할 때 쓰는 숫자"). STANDARD 이상은 기간을 골라 시간대·요일·상품별·재참여를
본다.

시간대 그래프는 계열이 하나라 **색도 하나**다. 값이 클수록 진하게 칠하는 방식은 막대 길이가 이미
말하는 것을 색으로 한 번 더 말하는 것이라 쓰지 않았다. 대신 가장 붐빈 시간 하나만 강조하고 그 시간을
문장으로 먼저 적는다. 사장이 이 화면에서 실제로 가져가는 정보가 그 한 시간이다. 강조는 색상이 아니라
명도 차라 색각 이상에서도 유지된다.

**AI** — 네 기능을 같은 카드로 반복하지 않는다. 리포트는 읽는 것, 개선 제안은 실행 목록, 문구는
입력을 받아 만드는 것, 매니저는 대화다. 형태를 기능에 맞췄고, 문구 생성만 바텀시트로 뺐다.

**요금제** — 3단 가격표에서 벗어났다. "모든 요금제에 들어 있는 것"이 비교표보다 먼저 온다. 사장이 이
화면에서 가장 먼저 걱정하는 것은 "돈을 안 내면 손님이 못 노나?"이고, 답이 "그런 일은 없다"이기
때문이다.

---

## 8. 법무 검토가 필요한 항목

지시의 `final_instruction`에 따라, 추측하지 않고 남긴다.

1. **요금제별 분석 데이터 보관기간(90/365일/장기)의 고지 방법.** 현재 요금제 화면에 적어 두었으나,
   개인정보 처리방침 본문에 반영할지와 문구는 정하지 않았다.
2. **"장기 집계"(PRO)의 실제 상한.** 코드상 상한이 없다. 무기한 보관이 허용되는지, 비식별 집계라도
   상한을 두어야 하는지 확인이 필요하다.
3. **OpenAI로 나가는 데이터의 국외 이전 고지.** 매장명과 공개 상품명이 국외 서버로 전송된다. 고객
   개인정보는 아니지만 사업자 정보이므로 이용약관에 근거가 필요한지 확인이 필요하다.
4. **AI 생성 문구의 책임 소재.** 모델이 만든 안내 문구를 매장이 그대로 게시했을 때, 그 표현에 대한
   책임이 누구에게 있는지 약관에 정해야 한다.
5. **결제 도입 시 등급 하향과 환불 규칙.** 현재는 하향 시 데이터가 지워지지 않고 조회만 제한되는데,
   이 동작이 환불·해지 규정과 맞는지 확인이 필요하다.

---

## 9. 알려진 한계

1. **PG 결제 미연동.** 지시 범위 밖이다. 등급 변경은 `SYSTEM_ADMIN`만 하고, 매장 화면의 "변경 요청"
   버튼은 권한이 없으면 안내 메시지를 띄운다. 결제를 붙일 때 `SubscriptionService.changePlan` 안쪽만
   결제 결과에 연결하면 된다.

2. **AI_CHAT 한 번에 최대 6회 공급자 왕복.** 한도는 1회로 센다. 토큰은 정확히 합산되지만 호출 횟수
   기준 비용은 최대 6배까지 과소 계상될 수 있다. 토큰 기준 상한이 필요하면 별도 작업이다.

3. **OpenAI 실호출 미검증.** CI와 로컬 모두 FakeLlmProvider로 돌았다. Responses API의 요청·응답
   모양은 코드로 구현했으나 실제 키로 확인하지 않았다. 특히 도구 왕복(`function_call` /
   `function_call_output`) 경로는 실제 API에서 한 번 확인해야 한다.

4. **브라우저 시각 검수 미완.** 작업 후반에 브라우저 확장이 끊겨 새 화면 세 개(AI·요금제·통계)를 실제
   렌더로 보지 못했다. 빌드·번들 격리·E2E·색 대비는 계산으로 확인했으나 레이아웃은 눈으로 보지
   않았다. 폰에서 한 번 확인이 필요하다.

5. **주간 리포트 스케줄러가 단일 인스턴스 전제.** 인스턴스가 늘면 같은 매장 리포트가 중복 생성된다.
   PIN·로그인 시도 제한과 같은 제약이다.

6. **`admin_users.login_id` 컬럼 잔존.** `ddl-auto=update`가 못 지운다. nullable이고 쓰는 코드가 없다.

7. **자유 입력 PII 차단은 형태 기반.** 전화번호·이메일 형태를 잡는다. "김철수 손님" 같은 이름은 막지
   못한다. 이름은 형태로 구분할 수 없어서, 여기서 더 나아가려면 별도 판단이 필요하다.

---

## 10. 되돌리면 안 되는 지점

`CLAUDE.md`에 근거와 함께 기록했다. 요약하면:

- `Entitlement` enum에 게임 관련 항목을 넣지 말 것
- 공급자 선택을 `@ConditionalOnProperty`로 되돌리지 말 것 (오타 하나로 앱이 안 뜬다)
- 모델 호출을 `@Transactional` 안에 넣지 말 것 (커넥션 풀이 마른다)
- 한도 차감·환급을 읽고-쓰기로 바꾸지 말 것
- 되돌릴 수 있는 실패는 공급자에 닿기 전 것뿐
- 관리자 자유 입력은 `withoutPersonalData`를 지날 것
- 등급 변경에서 멤버십 검사를 운영자 검사보다 먼저 두지 말 것
- 요금제·AI 문자열을 공용 프런트 모듈에 두지 말 것 (고객 번들로 샌다)
- 분석 보관기간과 개인정보 보존 120일을 한 값으로 합치지 말 것

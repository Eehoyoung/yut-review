"use client";
import { FormEvent, useState } from "react";
import { useParams } from "next/navigation";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { api, errorMessage } from "@/lib/api";
import type { Coupon } from "@/types/api";
export default function CouponPage(){const token=String(useParams().couponToken),qc=useQueryClient(),[open,setOpen]=useState(false),[pin,setPin]=useState("");const q=useQuery({queryKey:["coupon",token],queryFn:()=>api<Coupon>(`/public/coupons/${encodeURIComponent(token)}`)});const redeem=useMutation({mutationFn:()=>api<Coupon>(`/public/coupons/${encodeURIComponent(token)}/redeem`,{method:"POST",body:JSON.stringify({pin})}),onSuccess:()=>{setOpen(false);setPin("");qc.invalidateQueries({queryKey:["coupon",token]})}});if(q.isPending)return <main className="shell">쿠폰을 불러오는 중...</main>;if(q.isError)return <main className="shell"><p className="error">{errorMessage(q.error)}</p></main>;const c=q.data,name=c.prizeName??c.prize?.name??"당첨 상품",notYet=c.status==="ISSUED"&&new Date(c.validFrom)>new Date();return <main className="shell"><p className="brand">MY COUPON</p><h1>{name}</h1><div className="card"><span className="pill">{notYet?"아직 사용 불가":c.status==="ISSUED"?"사용 가능":c.status==="REDEEMED"?"사용 완료":c.status==="EXPIRED"?"기간 만료":"사용 취소"}</span><p>{c.prizeDescription??c.prize?.description}</p><p><b>사용 가능일</b><br/>{new Date(c.validFrom).toLocaleString("ko-KR")}</p><p><b>만료일</b><br/>{new Date(c.expiresAt).toLocaleString("ko-KR")}</p></div>{c.status==="ISSUED"&&<button className="btn secondary" disabled={notYet} onClick={()=>setOpen(true)}>직원용 사용 처리</button>}{open&&<div className="dialog-backdrop" role="presentation"><form className="dialog" role="dialog" aria-modal="true" aria-labelledby="redeem-title" onSubmit={(e:FormEvent)=>{e.preventDefault();redeem.mutate()}}><h2 id="redeem-title">정말 사용 처리할까요?</h2><p className="lead">직원이 직접 6자리 PIN을 입력해주세요. 완료 후 되돌릴 수 없습니다.</p><div className="field"><label htmlFor="redeem-pin">직원 PIN</label><input id="redeem-pin" type="password" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,""))}/></div>{redeem.isError&&<p className="error">{errorMessage(redeem.error)}</p>}<div className="row"><button type="button" className="btn ghost" onClick={()=>setOpen(false)}>취소</button><button className="btn" disabled={redeem.isPending||pin.length!==6}>사용 완료</button></div></form></div>}</main>}
